# Local API origin: http://localhost:5000. HOST and PORT are optional and
# these are the server's own defaults.
HOST=0.0.0.0
PORT=5000

# Same secret as apps/web/.env: both mounts sign sessions over one user store.
BETTER_AUTH_SECRET=replace-with-a-32-character-secret

# The API origin; keep it in step with HOST and PORT. Its session cookie is
# host-only, separate from the Next.js cookie on port 4000.
BETTER_AUTH_URL=http://localhost:5000

# Comma-separated browser origins allowed to send credentialed requests.
CLIENT_ORIGINS=http://localhost:4000

# Optional: on or off. Unset, Better Auth throttles sign-in and sign-up only
# under NODE_ENV=production.
# AUTH_RATE_LIMIT=on

DATABASE_URL=postgresql://postgres:password@localhost:5432/bookkeeping
