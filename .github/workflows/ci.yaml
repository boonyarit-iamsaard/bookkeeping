name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    name: CI
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v7.0.1

      # packages/database needs no .env here: the test harness injects the
      # Testcontainers DATABASE_URL into the db:push child process.
      - name: Configure CI environment
        run: cp apps/web/.env.ci.example apps/web/.env

      - name: Enable corepack
        run: corepack enable

      - uses: actions/setup-node@v7.0.0
        with:
          node-version: 24
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - name: Install Playwright browsers
        run: pnpm --filter @bookkeeping/web exec playwright install --with-deps chromium

      - name: Run CI checks
        run: pnpm run ci
