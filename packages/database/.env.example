# Used only by the Drizzle CLI (`pnpm db:push`, `pnpm db:studio`). Runtime
# consumers inject their own validated DATABASE_URL. This value points at the
# local PostgreSQL from docker-compose.yaml, whose fixed credentials it matches.
DATABASE_URL=postgresql://postgres:password@localhost:5432/bookkeeping
