# Used only by the Drizzle CLI (`pnpm db:push`, `pnpm db:studio`). Runtime
# consumers inject their own validated DATABASE_URL; keep this value in step
# with apps/server/.env and POSTGRES_PASSWORD in the root .env.local.
DATABASE_URL=postgresql://postgres:password@localhost:5432/bookkeeping
