# Local infrastructure (Docker Compose) overrides. Git-ignored; copy
# .env.local.example to .env.local to get started.
#
# Keep POSTGRES_PASSWORD in sync with DATABASE_URL in apps/legacy-web/.env — the
# app and the local database must agree on the password.
POSTGRES_PASSWORD=password
