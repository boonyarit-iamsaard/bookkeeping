# Local infrastructure (Docker Compose) overrides. Git-ignored; copy
# .env.local.example to .env.local to get started.
#
# Keep POSTGRES_PASSWORD in sync with DATABASE_URL in apps/server/.env and
# packages/database/.env — the API, CLI, and local database must agree on it.
POSTGRES_PASSWORD=password
