# 09: Edit an existing category from the picker

Read `../spec.md` first (Categories).

**What to build:** While recording a transaction, the account holder can
rename, re-icon or remove an existing category from the category picker
without losing the transaction they are entering.

**Blocked by:** 07 (Avatar menu, the Categories entry, and the milestone check)

**Status:** needs-triage

**Out of scope:** creating categories from the picker, which already works
inline with the icon picker.

- [ ] Deferred during 07's grilling (2026-09-24): the picker creates categories inline but has never linked to Categories, and a plain link would drop the draft. Revisit only if editing mid-entry turns out to be needed; the likely shape is the editor sheet opened over the entry form, keeping the draft.

## Comments
