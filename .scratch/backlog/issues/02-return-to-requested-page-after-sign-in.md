# 02: Return to the requested page after sign-in

Read `../../phone-first-redesign/spec.md` first (Routes).

**What to build:** A signed-out visit to a signed-in address, such as
`/reports?month=2026-09`, lands back on that address after sign-in instead
of on Home.

**Blocked by:** phone-first-redesign 07, done (Avatar menu, the Categories entry, and the milestone check)

**Status:** needs-triage

**Out of scope:** sign-up, which starts a new Account and lands on Home.

- [ ] Raised during 07's grilling (2026-09-24), which sends every sign-in to Home. The return address must be internal only; the capture-origin resolver from 04 is the likely prior art.

## Comments

### 2026-09-24: deferred to the backlog

> _This was generated by AI during triage._

Moved out of `phone-first-redesign` (where it was 10) because the
maintainer doesn't expect to want this behaviour soon. It is a deferral, not a
rejection, so nothing was written to `.out-of-scope/`. It stays
`needs-triage`; run `/triage` on `.scratch/backlog/` to pick it back up.
